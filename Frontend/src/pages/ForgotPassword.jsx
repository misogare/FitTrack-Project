import { useState } from 'react';
import { Link } from 'react-router-dom';
import Navbar from '../components/Navbar';
import Icon from '../components/Icon';

export default function ForgotPassword(){const [email,setEmail]=useState('');const [sent,setSent]=useState(false);return <><Navbar publicPage/><div className="auth-page"><div className="auth-stack"><div className="auth-card"><div className="auth-header"><div className="auth-mark">ϟ</div><h1>Reset your password</h1><p>Enter your email and we'll help you get back into FitTrack.</p></div><div className="auth-divider"/>{sent?<div className="alert">If an account exists for <strong>{email}</strong>, a reset link would be sent here. The reset-link flow is ready in the UI; the current backend does not expose a password-reset endpoint yet.</div>:<form className="auth-form" onSubmit={e=>{e.preventDefault();setSent(true)}}><label className="field"><span className="field-label">Email Address</span><input type="email" required placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)}/><span className="field-hint">Reset links should be time-limited for account security.</span></label><div className="alert"><Icon name="info" size={14}/> Reset links are intended to expire after 1 hour.</div><button className="button button-dark button-full"><Icon name="mail" size={14}/> Send Reset Link</button></form>}<div className="auth-foot"><Link className="auth-link" to="/login">Back to login</Link></div></div></div></div></>}
